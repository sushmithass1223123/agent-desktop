import { QuillDirective } from './quill.directive';

describe('QuillDirective', () => {
  it('should create an instance', () => {
    const directive = new QuillDirective();
    expect(directive).toBeTruthy();
  });
});
