import { TChartDirective } from './t-chart.directive';

describe('TChartDirective', () => {
  it('should create an instance', () => {
    const directive = new TChartDirective();
    expect(directive).toBeTruthy();
  });
});
