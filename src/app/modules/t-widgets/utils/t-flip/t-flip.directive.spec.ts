import { TFlipDirective } from './t-flip.directive';

describe('TFlipDirective', () => {
  it('should create an instance', () => {
    const directive = new TFlipDirective();
    expect(directive).toBeTruthy();
  });
});
